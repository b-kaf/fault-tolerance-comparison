# Normalize all text files to LF on checkout and commit, regardless of
# the contributor's core.autocrlf setting. Required because Nix embeds
# shell scripts verbatim from files in the working tree; CRLF line
# endings produce `$'\r': command not found` build failures.
* text=auto eol=lf
