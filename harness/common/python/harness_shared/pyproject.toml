[project]
name = "harness-shared"
version = "0.1.0"
description = "Shared Python helpers for fault-tolerance harness tooling"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"
