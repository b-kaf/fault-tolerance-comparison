#ifndef HARNESS_ABI_H
#define HARNESS_ABI_H

#include <stdint.h>

enum {
    HARNESS_STAGE_BOOT = 0,
    HARNESS_STAGE_AFTER_INIT = 1,
    HARNESS_STAGE_BEFORE_READ = 2,
    HARNESS_STAGE_AFTER_READ = 3,
    HARNESS_STAGE_AFTER_CHECKPOINT = 4,
    HARNESS_STAGE_AFTER_MUTATION = 5,
    HARNESS_STAGE_BEFORE_COMMIT = 6,
    HARNESS_STAGE_AFTER_COMMIT = 7,
    HARNESS_STAGE_BEFORE_RECOVERY = 8,
    HARNESS_STAGE_AFTER_PRIMARY = 9,
    HARNESS_STAGE_AFTER_ALTERNATE = 10,
    HARNESS_STAGE_AFTER_RECOVERY = 11,
    HARNESS_STAGE_BEFORE_CONTROL_FLOW = 12,
    HARNESS_STAGE_AFTER_CONTROL_READ = 13,
    HARNESS_STAGE_AFTER_CONTROL_COMPUTE = 14,
    HARNESS_STAGE_AFTER_CONTROL_FLOW = 15,
    HARNESS_STAGE_BEFORE_WORKFLOW = 16,
    HARNESS_STAGE_AFTER_WORKFLOW = 17,
};

enum {
    HARNESS_FAULT_NONE = 0,
    HARNESS_FAULT_COPY_A = 1,
    HARNESS_FAULT_ALL_DISTINCT = 2,
    HARNESS_FAULT_ACTIVE_VALUE = 10,
    HARNESS_FAULT_ACTIVE_LENGTH = 11,
    HARNESS_FAULT_ACTIVE_CHECKSUM = 12,
    HARNESS_FAULT_CHECKPOINT_VALUE = 13,
    HARNESS_FAULT_CHECKPOINT_CHECKSUM = 14,
    HARNESS_FAULT_ACTIVE_VALUE_AND_CHECKPOINT_CHECKSUM = 15,
    HARNESS_FAULT_RECOVERY_PRIMARY_VALUE = 20,
    HARNESS_FAULT_RECOVERY_PRIMARY_CHECKSUM = 21,
    HARNESS_FAULT_RECOVERY_PRIMARY_VALUE_AND_ALTERNATE_CHECKSUM = 22,
    HARNESS_FAULT_RECOVERY_PRIMARY_VALUE_AND_CHECKPOINT_CHECKSUM = 23,
    HARNESS_FAULT_CONTROL_PHASE = 30,
    HARNESS_FAULT_CONTROL_SIGNATURE = 31,
    HARNESS_FAULT_CONTROL_SKIP_COMPUTE = 32,
    HARNESS_FAULT_CONTROL_REPEAT_READ = 33,
    HARNESS_FAULT_CONTROL_EARLY_TERMINAL = 34,
};

enum {
    HARNESS_STATUS_OK = 0,
    HARNESS_STATUS_NO_MAJORITY = 1,
};

enum {
    HARNESS_RESTART_COMMITTED = 0,
    HARNESS_RESTART_RESTORED = 1,
    HARNESS_RESTART_RESTORE_FAILED = 2,
};

enum {
    HARNESS_RECOVERY_PRIMARY_ACCEPTED = 0,
    HARNESS_RECOVERY_ALTERNATE_ACCEPTED = 1,
    HARNESS_RECOVERY_UNRECOVERABLE = 2,
    HARNESS_RECOVERY_CHECKPOINT_FAILED = 3,
    HARNESS_RECOVERY_RESTORE_FAILED = 4,
};

enum {
    HARNESS_CONTROL_OK = 0,
    HARNESS_CONTROL_INVALID_TRANSITION = 1,
    HARNESS_CONTROL_BAD_SIGNATURE = 2,
    HARNESS_CONTROL_UNEXPECTED_TERMINAL = 3,
};

/* Workflow outcome, used by the combined/baseline harnesses. A pass is any
 * outcome other than CORRECT-but-wrong: the correct committed output, a
 * recovered value, or a safe detected stop. SDC means a wrong value was
 * committed without detection. */
enum {
    HARNESS_OUTCOME_CORRECT = 0,
    HARNESS_OUTCOME_RECOVERED = 1,
    HARNESS_OUTCOME_SAFE_STOP = 2,
    HARNESS_OUTCOME_SDC = 3,
};

#endif
